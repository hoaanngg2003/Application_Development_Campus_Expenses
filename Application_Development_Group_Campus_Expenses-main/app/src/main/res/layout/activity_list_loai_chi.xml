<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="vn.lvc.quanlychitieu.ListLoaiChiActivity">
    <include
        android:id="@+id/toolbarListLoaiChi"
        layout="@layout/custom_toolbar2"
        />

    <ListView
        android:id="@+id/lvLoaiChi"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/a"
        android:layout_below="@id/toolbarListLoaiChi"
        android:layout_marginBottom="8dp" />
    <LinearLayout
        android:id="@+id/a"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentBottom="true">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="them"
            android:text="Add new expense" />
    </LinearLayout>
</RelativeLayout>